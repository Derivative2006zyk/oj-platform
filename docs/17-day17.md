# 第 17 天：插件配置持久化与事件发布接入

### 一、今日目标

- 新增 plugin_configs 表
- Alembic 迁移
- PluginRegistry 从 DB 加载启用状态
- problem service 层发布事件
- 新增插件管理 API
- 新增测试

### 二、plugin_configs 模型

```python
class PluginConfig(Base):
    __tablename__ = "plugin_configs"
    id: Mapped[int] = ...
    plugin_name: Mapped[str] = ... unique
    enabled: Mapped[bool] = default True
    config: Mapped[dict] = JSONB default {}
    created_at: DateTime
    updated_at: DateTime
```

### 三、插件配置存储

- `ensure_config`：不存在则创建
- `set_enabled`：切换启用状态
- `update_config`：更新 config

### 四、注册表改造

- `load_all` 从 DB 读 enabled
- `disable` 回写 DB
- `enable` 回写 DB

### 五、事件发布接入

service 层：

```python
await get_event_bus().publish(
    EventNames.PROBLEM_CREATED,
    {"id": problem.id, "title": problem.title},
)
```

### 六、插件管理 API

- `GET /api/admin/plugins`
- `PUT /api/admin/plugins/{name}/enable`
- `PUT /api/admin/plugins/{name}/disable`

### 七、测试

- test_plugin_config.py：7 个
- test_problem_events.py：1 个
- 总计 32 个测试通过

### 八、注意事项

#### 1、事件发布必须在 commit 之后

#### 2、disable 不卸载路由

#### 3、service 层不依赖注册表

#### 4、autogenerate 不生成 server_default

### 九、Git 提交

见提交记录。

### 十、验收清单

- [x] plugin_configs 表创建
- [x] 迁移执行
- [x] 插件配置存储实现
- [x] 注册表从 DB 加载
- [x] service 层发布事件
- [x] 管理 API 可用
- [x] 32 个测试通过
- [x] 0.1 功能未受影响
- [x] Git 提交完成

### 十一、常见问题

| 现象 | 原因 | 解决 |
|---|---|---|
| 迁移未生成 | 模型未导入 | 检查 models/__init__.py |
| DB 无记录 | load_all 未调 _sync | 检查代码 |
| 事件未发 | service 未改 | 检查 commit 后的 publish |
| 管理 API 404 | 路由未挂 | 检查 get_routers |