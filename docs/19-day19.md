# 第 19 天：前端登录注册页与路由守卫

### 一、今日目标

- 安装 Pinia
- 统一 axios 实例 + token 拦截器
- 用户 API 封装
- Pinia 用户状态
- 登录页、注册页
- 顶部导航栏
- 路由守卫
- 手动验证流程

### 二、Pinia Store

```typescript
export const useUserStore = defineStore('user', () => {
  const token = ref(localStorage.getItem('oj_token') || '')
  const user = ref<UserInfo | null>(null)

  const isLoggedIn = computed(() => !!token.value)
  const isAdmin = computed(() => user.value?.role === 'admin')

  async function login(payload) {
    const res = await authApi.login(payload)
    token.value = res.access_token
    localStorage.setItem('oj_token', res.access_token)
    const u = await fetchMe()
    return u
  }

  function logout() {
    token.value = ''
    user.value = null
    localStorage.removeItem('oj_token')
    localStorage.removeItem('oj_user')
  }

  return { token, user, isLoggedIn, isAdmin, login, logout, ... }
})
```

### 三、axios 拦截器

```typescript
api.interceptors.request.use((config) => {
  const token = localStorage.getItem('oj_token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

api.interceptors.response.use(
  (res) => res,
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem('oj_token')
      if (!window.location.pathname.startsWith('/login')) {
        window.location.href = '/login'
      }
    }
    return Promise.reject(error)
  }
)
```

### 四、路由守卫

```typescript
router.beforeEach((to, from, next) => {
  const token = localStorage.getItem('oj_token')

  if (to.meta.requiresAuth && !token) {
    next({ path: '/login', query: { redirect: to.fullPath } })
    return
  }

  if (to.meta.guestOnly && token) {
    next('/')
    return
  }

  next()
})
```

### 五、登录页逻辑

- `form.username`、`form.password`
- 提交 → `userStore.login()` → 成功跳 `redirect || '/'`
- 401 显示"用户名或密码错误"

### 六、路由 meta

| 路由 | meta |
|---|---|
| `/` | 无 |
| `/problem/:id` | 无 |
| `/login` | `guestOnly: true` |
| `/register` | `guestOnly: true` |
| `/admin/problem/new` | `requiresAuth: true` |
| `/admin/problem/:id/edit` | `requiresAuth: true` |

### 七、注意事项

#### 1、Pinia 在 router 之前 use

```typescript
app.use(createPinia())
app.use(router)
```

#### 2、路由守卫用 localStorage

不用 store，避免初始化顺序问题。

#### 3、axios 拦截器不用 router

用 `window.location.href` 避免循环依赖。

#### 4、登录先存 token 再 fetchMe

`/users/me` 需要 Authorization。

#### 5、401 非登录页才跳

避免死循环。

### 八、手动验证

- 注册 charlie → 跳登录
- 登录 charlie → 回首页
- 刷新保持登录
- 未登录访问 `/admin/problem/new` → 跳登录
- 登出清空 token
- 手动改 role=admin → 导航显示"管理员"徽标 + "新建题目"
- 0.1 列表、详情、图片正常

### 九、Git 提交

见提交记录。

### 十、验收清单

- [x] Pinia 安装并挂载
- [x] axios 统一实例与拦截器
- [x] 用户 API 与 store
- [x] 登录注册页
- [x] 导航栏
- [x] 路由守卫
- [x] 刷新保持登录
- [x] 401 自动跳登录
- [x] 0.1 功能未受影响
- [x] Git 提交完成